# Copy to .env. The deployed website/CMS the app reads from.
# Use your live domain in production, or a tunnel/LAN URL while developing
# against a local site (e.g. an ngrok URL or http://192.168.x.x:3000).
EXPO_PUBLIC_API_BASE=https://kingstonmosque.org
